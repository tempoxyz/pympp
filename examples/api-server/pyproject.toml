[project]
name = "api-server-example"
version = "0.1.0"
description = "Payment-protected API server example using the Machine Payments Protocol"
requires-python = ">=3.12"
dependencies = [
    "pympp[tempo,server]",
    "fastapi",
    "uvicorn",
]

[tool.uv.sources]
pympp = { path = "../.." }
